# Changelog

## Unreleased
- Live follow-up prompt queue (Claude Code parity): input stays live while working; follow-ups appear in transient "⏳ Queued (n)" region above the box (not in permanent transcript); processed at next turn boundary with correct ordering; no polluting banners; /clear clears queue. Includes E2E PTY seam + test, transient UI in Ink App, history defer for queued turns, and minimal marketing updates (topbar + install block on ero.solar, README, package.json).

## [1.0.61] - 2026-01-25

### Features\n- Update release 1.0.61\n\n### Bug Fixes\n- Minor improvements and optimizations

